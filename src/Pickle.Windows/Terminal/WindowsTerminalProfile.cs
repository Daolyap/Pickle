namespace Pickle.Windows.Terminal;

/// <summary>
/// Windows Terminal integration (JSON fragment with a Pickle profile + color scheme).
/// FOUNDATION PLACEHOLDER — workstream W3 implements Install/Uninstall/Update and `pk terminal`.
/// </summary>
public static class WindowsTerminalProfile
{
    public static int Install(string executablePath) => 1;

    public static int Uninstall() => 1;
}
